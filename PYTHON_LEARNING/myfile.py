def my_function():
    print("To import you own package, first create a file named myfile.py in the same directory as the script or in Python's module search path.")
    print(" The custom module named myfile likely contains at least one function called my_function().")
    print("Then call the function named my_function() that is defined within the myfile module.")
    
my_function()